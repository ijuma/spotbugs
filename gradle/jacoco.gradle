apply plugin: "jacoco"

jacoco {
  toolVersion = "0.8.2"
}

jacocoTestReport {
  reports {
    html.enabled = true
  }
}

test {
  jacoco {
    append = true
    destinationFile = project(':spotbugs').file('build/jacoco/test.exec')
    includeNoLocationClasses = true
  }
}
